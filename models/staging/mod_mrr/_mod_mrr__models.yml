version: 2

models:
  - name: stg_mod_mrr__subscription_periods
    description: This pulls the subscription periods data into our model
    columns:
      - name: subscription_id
        description: Primary key
        tests:
          - unique
          - not_null